/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://bilalahmedbaf-03.netlify.app', // Replace with your actual site URL
    generateRobotsTxt: true, // Generate a robots.txt file
    sitemapSize: 5000, // Max entries per sitemap file (optional)
    changefreq: 'daily', // Change frequency of your pages (optional)
    priority: 0.7, // Priority of URLs (optional)
  }